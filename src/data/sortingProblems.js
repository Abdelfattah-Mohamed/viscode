/** Sorting-only catalog merged into PROBLEMS — category "Sorting", visualizer "sorting". */

const LM_BUBBLE = { init: 2, loop: 3, visit: 4, compare: 5, update: 6, done: 10 };
const LM_SELECT = { init: 2, loop: 3, visit: 4, compare: 5, update: 6, done: 11 };
const LM_INSERT = { init: 2, loop: 3, compare: 4, update: 5, visit: 6, done: 9 };
const LM_MERGE = { init: 2, loop: 3, visit: 4, compare: 7, update: 8, done: 11 };
const LM_COUNT = { init: 2, loop: 4, visit: 6, compare: 8, update: 11, done: 14 };
const LM_HEAP = { init: 2, loop: 3, visit: 4, compare: 5, update: 7, done: 10 };
const LM_RADIX = { init: 2, loop: 4, compare: 6, update: 10, visit: 9, done: 16 };
const LM_SHELL = { init: 2, loop: 3, visit: 4, compare: 8, update: 10, done: 13 };
const LM_QUICK = { init: 2, loop: 3, visit: 4, compare: 8, update: 11, done: 16 };
const LM_BUCKET = { init: 2, loop: 4, compare: 5, update: 11, visit: 10, done: 14 };
const LM_GAP = { init: 2, loop: 4, compare: 5, update: 6, visit: 5, done: 8 };

function langsBubble() {
  return {
    cpp: {
      code: [
        "void bubbleSort(vector<int>& nums) {",
        "  int n = nums.size();",
        "  for (int i = 0; i < n; i++) {",
        "    for (int j = 0; j < n - 1 - i; j++) {",
        "      if (nums[j] > nums[j + 1]) {",
        "        swap(nums[j], nums[j + 1]);",
        "      }",
        "    }",
        "  }",
        "}",
      ],
      lineMap: LM_BUBBLE,
    },
    java: {
      code: [
        "void bubbleSort(int[] nums) {",
        "  int n = nums.length;",
        "  for (int i = 0; i < n; i++) {",
        "    for (int j = 0; j < n - 1 - i; j++) {",
        "      if (nums[j] > nums[j + 1]) {",
        "        int t = nums[j]; nums[j] = nums[j + 1]; nums[j + 1] = t;",
        "      }",
        "    }",
        "  }",
        "}",
      ],
      lineMap: LM_BUBBLE,
    },
    javascript: {
      code: [
        "function bubbleSort(nums) {",
        "  const n = nums.length;",
        "  for (let i = 0; i < n; i++) {",
        "    for (let j = 0; j < n - 1 - i; j++) {",
        "      if (nums[j] > nums[j + 1]) {",
        "        [nums[j], nums[j + 1]] = [nums[j + 1], nums[j]];",
        "      }",
        "    }",
        "  }",
        "}",
      ],
      lineMap: LM_BUBBLE,
    },
    python: {
      code: [
        "def bubble_sort(nums):",
        "    n = len(nums)",
        "    for i in range(n):",
        "        for j in range(n - 1 - i):",
        "            if nums[j] > nums[j + 1]:",
        "                nums[j], nums[j + 1] = nums[j + 1], nums[j]",
        "",
      ],
      lineMap: { init: 2, loop: 3, visit: 4, compare: 5, update: 6, done: 7 },
    },
  };
}

function langsSelection() {
  return {
    cpp: {
      code: [
        "void selectionSort(vector<int>& nums) {",
        "  int n = nums.size();",
        "  for (int i = 0; i < n; i++) {",
        "    int m = i;",
        "    for (int j = i + 1; j < n; j++) {",
        "      if (nums[j] < nums[m]) {",
        "        m = j;",
        "      }",
        "    }",
        "    swap(nums[i], nums[m]);",
        "  }",
        "}",
      ],
      lineMap: LM_SELECT,
    },
    java: {
      code: [
        "void selectionSort(int[] nums) {",
        "  int n = nums.length;",
        "  for (int i = 0; i < n; i++) {",
        "    int m = i;",
        "    for (int j = i + 1; j < n; j++) {",
        "      if (nums[j] < nums[m]) {",
        "        m = j;",
        "      }",
        "    }",
        "    int t = nums[i]; nums[i] = nums[m]; nums[m] = t;",
        "  }",
        "}",
      ],
      lineMap: LM_SELECT,
    },
    javascript: {
      code: [
        "function selectionSort(nums) {",
        "  const n = nums.length;",
        "  for (let i = 0; i < n; i++) {",
        "    let m = i;",
        "    for (let j = i + 1; j < n; j++) {",
        "      if (nums[j] < nums[m]) {",
        "        m = j;",
        "      }",
        "    }",
        "    [nums[i], nums[m]] = [nums[m], nums[i]];",
        "  }",
        "}",
      ],
      lineMap: LM_SELECT,
    },
    python: {
      code: [
        "def selection_sort(nums):",
        "    n = len(nums)",
        "    for i in range(n):",
        "        m = i",
        "        for j in range(i + 1, n):",
        "            if nums[j] < nums[m]:",
        "                m = j",
        "        nums[i], nums[m] = nums[m], nums[i]",
        "",
      ],
      lineMap: { init: 2, loop: 3, visit: 4, compare: 5, update: 7, done: 9 },
    },
  };
}

function langsInsertion() {
  return {
    cpp: {
      code: [
        "void insertionSort(vector<int>& nums) {",
        "  int n = nums.size();",
        "  for (int i = 1; i < n; i++) {",
        "    int key = nums[i];",
        "    int j = i - 1;",
        "    while (j >= 0 && nums[j] > key) {",
        "      nums[j + 1] = nums[j];",
        "      j--;",
        "    }",
        "    nums[j + 1] = key;",
        "  }",
        "}",
      ],
      lineMap: LM_INSERT,
    },
    java: {
      code: [
        "void insertionSort(int[] nums) {",
        "  int n = nums.length;",
        "  for (int i = 1; i < n; i++) {",
        "    int key = nums[i];",
        "    int j = i - 1;",
        "    while (j >= 0 && nums[j] > key) {",
        "      nums[j + 1] = nums[j];",
        "      j--;",
        "    }",
        "    nums[j + 1] = key;",
        "  }",
        "}",
      ],
      lineMap: LM_INSERT,
    },
    javascript: {
      code: [
        "function insertionSort(nums) {",
        "  const n = nums.length;",
        "  for (let i = 1; i < n; i++) {",
        "    const key = nums[i];",
        "    let j = i - 1;",
        "    while (j >= 0 && nums[j] > key) {",
        "      nums[j + 1] = nums[j];",
        "      j--;",
        "    }",
        "    nums[j + 1] = key;",
        "  }",
        "}",
      ],
      lineMap: LM_INSERT,
    },
    python: {
      code: [
        "def insertion_sort(nums):",
        "    for i in range(1, len(nums)):",
        "        key = nums[i]",
        "        j = i - 1",
        "        while j >= 0 and nums[j] > key:",
        "            nums[j + 1] = nums[j]",
        "            j -= 1",
        "        nums[j + 1] = key",
        "",
      ],
      lineMap: { init: 2, loop: 3, compare: 5, update: 6, visit: 8, done: 9 },
    },
  };
}

function langsMerge() {
  return {
    cpp: {
      code: [
        "void mergeSortIter(vector<int>& nums) {",
        "  int n = nums.size();",
        "  for (int w = 1; w < n; w *= 2) {",
        "    for (int l = 0; l < n; l += 2 * w) {",
        "      int m = min(l + w, n);",
        "      int r = min(l + 2 * w, n);",
        "      if (m >= r) continue;",
        "      inplaceMerge(nums, l, m, r);",
        "    }",
        "  }",
        "}",
      ],
      lineMap: LM_MERGE,
    },
    java: {
      code: [
        "void mergeSortIter(int[] nums) {",
        "  int n = nums.length;",
        "  for (int w = 1; w < n; w *= 2) {",
        "    for (int l = 0; l < n; l += 2 * w) {",
        "      int m = Math.min(l + w, n);",
        "      int r = Math.min(l + 2 * w, n);",
        "      if (m >= r) continue;",
        "      inplaceMerge(nums, l, m, r);",
        "    }",
        "  }",
        "}",
      ],
      lineMap: LM_MERGE,
    },
    javascript: {
      code: [
        "function mergeSortIter(nums) {",
        "  const n = nums.length;",
        "  for (let w = 1; w < n; w *= 2) {",
        "    for (let l = 0; l < n; l += 2 * w) {",
        "      const m = Math.min(l + w, n);",
        "      const r = Math.min(l + 2 * w, n);",
        "      if (m >= r) continue;",
        "      inplaceMerge(nums, l, m, r);",
        "    }",
        "  }",
        "}",
      ],
      lineMap: LM_MERGE,
    },
    python: {
      code: [
        "def merge_sort_iter(nums):",
        "    n = len(nums)",
        "    w = 1",
        "    while w < n:",
        "        for l in range(0, n, 2 * w):",
        "            m = min(l + w, n)",
        "            r = min(l + 2 * w, n)",
        "            if m < r:",
        "                inplace_merge(nums, l, m, r)",
        "        w *= 2",
        "",
      ],
      lineMap: { init: 2, loop: 4, visit: 5, compare: 7, update: 9, done: 11 },
    },
  };
}

function langsCounting() {
  return {
    cpp: {
      code: [
        "void countingSort(vector<int>& nums, int shiftKey) {",
        "  int n = nums.size();",
        "  int maxK = 0;",
        "  for (int x : nums) maxK = max(maxK, x - shiftKey);",
        "  vector<int> cnt(maxK + 1), out(n);",
        "  for (int x : nums) cnt[x - shiftKey]++;",
        "  for (int i = 1; i <= maxK; i++) cnt[i] += cnt[i - 1];",
        "  for (int i = n - 1; i >= 0; i--) {",
        "    int k = nums[i] - shiftKey;",
        "    cnt[k]--;",
        "    out[cnt[k]] = nums[i];",
        "  }",
        "  nums = out;",
        "}",
      ],
      lineMap: LM_COUNT,
    },
    java: {
      code: [
        "void countingSort(int[] nums, int shiftKey) {",
        "  int n = nums.length;",
        "  int maxK = 0;",
        "  for (int x : nums) maxK = Math.max(maxK, x - shiftKey);",
        "  int[] cnt = new int[maxK + 1];",
        "  int[] out = new int[n];",
        "  for (int x : nums) cnt[x - shiftKey]++;",
        "  for (int i = 1; i <= maxK; i++) cnt[i] += cnt[i - 1];",
        "  for (int i = n - 1; i >= 0; i--) {",
        "    int k = nums[i] - shiftKey;",
        "    cnt[k]--;",
        "    out[cnt[k]] = nums[i];",
        "  }",
        "  System.arraycopy(out, 0, nums, 0, n);",
        "}",
      ],
      lineMap: LM_COUNT,
    },
    javascript: {
      code: [
        "function countingSort(nums, shiftKey) {",
        "  const n = nums.length;",
        "  let maxK = 0;",
        "  for (const x of nums) maxK = Math.max(maxK, x - shiftKey);",
        "  const cnt = Array(maxK + 1).fill(0);",
        "  const out = Array(n);",
        "  for (const x of nums) cnt[x - shiftKey]++;",
        "  for (let i = 1; i <= maxK; i++) cnt[i] += cnt[i - 1];",
        "  for (let i = n - 1; i >= 0; i--) {",
        "    const k = nums[i] - shiftKey;",
        "    cnt[k]--;",
        "    out[cnt[k]] = nums[i];",
        "  }",
        "  for (let i = 0; i < n; i++) nums[i] = out[i];",
        "}",
      ],
      lineMap: LM_COUNT,
    },
    python: {
      code: [
        "def counting_sort(nums, shift_key):",
        "    n = len(nums)",
        "    keys = [x - shift_key for x in nums]",
        "    mx = max(keys)",
        "    cnt = [0] * (mx + 1)",
        "    out = [0] * n",
        "    for k in keys:",
        "        cnt[k] += 1",
        "    for i in range(1, mx + 1):",
        "        cnt[i] += cnt[i - 1]",
        "    for i in range(n - 1, -1, -1):",
        "        k = keys[i]",
        "        cnt[k] -= 1",
        "        out[cnt[k]] = nums[i]",
        "    nums[:] = out",
        "",
      ],
      lineMap: { init: 2, loop: 8, visit: 13, compare: 11, update: 15, done: 15 },
    },
  };
}

function langsHeap() {
  return {
    cpp: {
      code: [
        "void heapSort(vector<int>& nums) {",
        "  int n = nums.size();",
        "  for (int i = n / 2 - 1; i >= 0; i--)",
        "    siftDown(nums, n, i);",
        "  for (int end = n - 1; end > 0; end--) {",
        "    swap(nums[0], nums[end]);",
        "    siftDown(nums, end, 0);",
        "  }",
        "}",
      ],
      lineMap: LM_HEAP,
    },
    java: {
      code: [
        "void heapSort(int[] nums) {",
        "  int n = nums.length;",
        "  for (int i = n / 2 - 1; i >= 0; i--)",
        "    siftDown(nums, n, i);",
        "  for (int end = n - 1; end > 0; end--) {",
        "    swap(nums, 0, end);",
        "    siftDown(nums, end, 0);",
        "  }",
        "}",
      ],
      lineMap: LM_HEAP,
    },
    javascript: {
      code: [
        "function heapSort(nums) {",
        "  const n = nums.length;",
        "  for (let i = Math.floor(n / 2) - 1; i >= 0; i--)",
        "    siftDown(nums, n, i);",
        "  for (let end = n - 1; end > 0; end--) {",
        "    [nums[0], nums[end]] = [nums[end], nums[0]];",
        "    siftDown(nums, end, 0);",
        "  }",
        "}",
      ],
      lineMap: LM_HEAP,
    },
    python: {
      code: [
        "def heap_sort(nums):",
        "    n = len(nums)",
        "    for i in range(n // 2 - 1, -1, -1):",
        "        sift_down(nums, n, i)",
        "    for end in range(n - 1, 0, -1):",
        "        nums[0], nums[end] = nums[end], nums[0]",
        "        sift_down(nums, end, 0)",
        "",
      ],
      lineMap: { init: 2, loop: 3, visit: 4, compare: 5, update: 6, done: 8 },
    },
  };
}

function langsRadix() {
  return {
    cpp: {
      code: [
        "void radixSort(vector<int>& nums) {",
        "  int n = nums.size();",
        "  int maxv = *max_element(nums.begin(), nums.end());",
        "  for (int exp = 1; maxv / exp > 0; exp *= 10) {",
        "    vector<int> bucket[10];",
        "    for (int x : nums)",
        "      bucket[(x / exp) % 10].push_back(x);",
        "    int k = 0;",
        "    for (int d = 0; d < 10; d++)",
        "      for (int x : bucket[d]) nums[k++] = x;",
        "  }",
        "}",
      ],
      lineMap: LM_RADIX,
    },
    java: {
      code: [
        "void radixSort(int[] nums) {",
        "  int n = nums.length;",
        "  int maxv = Arrays.stream(nums).max().getAsInt();",
        "  for (int exp = 1; maxv / exp > 0; exp *= 10) {",
        "    ArrayList<Integer>[] buckets = new ArrayList[10];",
        "    for (int x : nums)",
        "      buckets[(x / exp) % 10].add(x);",
        "    int k = 0;",
        "    for (int d = 0; d < 10; d++)",
        "      for (int x : buckets[d]) nums[k++] = x;",
        "  }",
        "}",
      ],
      lineMap: LM_RADIX,
    },
    javascript: {
      code: [
        "function radixSort(nums) {",
        "  const n = nums.length;",
        "  let maxv = Math.max(...nums);",
        "  for (let exp = 1; Math.floor(maxv / exp) > 0; exp *= 10) {",
        "    const buckets = Array.from({ length: 10 }, () => []);",
        "    for (const x of nums)",
        "      buckets[Math.floor(x / exp) % 10].push(x);",
        "    let k = 0;",
        "    for (let d = 0; d < 10; d++)",
        "      for (const x of buckets[d]) nums[k++] = x;",
        "  }",
        "}",
      ],
      lineMap: LM_RADIX,
    },
    python: {
      code: [
        "def radix_sort(nums):",
        "    mx = max(nums)",
        "    exp = 1",
        "    while mx // exp > 0:",
        "        buckets = [[] for _ in range(10)]",
        "        for x in nums:",
        "            buckets[(x // exp) % 10].append(x)",
        "        k = 0",
        "        for b in buckets:",
        "            for x in b:",
        "                nums[k] = x",
        "                k += 1",
        "        exp *= 10",
        "",
      ],
      lineMap: { init: 2, loop: 4, compare: 7, update: 11, visit: 10, done: 14 },
    },
  };
}

function langsShell() {
  return {
    cpp: {
      code: [
        "void shellSort(vector<int>& nums) {",
        "  int n = nums.size();",
        "  for (int gap = n / 2; gap > 0; gap /= 2) {",
        "    for (int i = gap; i < n; i++) {",
        "      int temp = nums[i];",
        "      int j;",
        "      for (j = i; j >= gap && nums[j - gap] > temp; j -= gap)",
        "        nums[j] = nums[j - gap];",
        "      nums[j] = temp;",
        "    }",
        "  }",
        "}",
      ],
      lineMap: LM_SHELL,
    },
    java: {
      code: [
        "void shellSort(int[] nums) {",
        "  int n = nums.length;",
        "  for (int gap = n / 2; gap > 0; gap /= 2) {",
        "    for (int i = gap; i < n; i++) {",
        "      int temp = nums[i];",
        "      int j = i;",
        "      while (j >= gap && nums[j - gap] > temp) {",
        "        nums[j] = nums[j - gap];",
        "        j -= gap;",
        "      }",
        "      nums[j] = temp;",
        "    }",
        "  }",
        "}",
      ],
      lineMap: LM_SHELL,
    },
    javascript: {
      code: [
        "function shellSort(nums) {",
        "  const n = nums.length;",
        "  for (let gap = Math.floor(n / 2); gap > 0; gap = Math.floor(gap / 2)) {",
        "    for (let i = gap; i < n; i++) {",
        "      const temp = nums[i];",
        "      let j = i;",
        "      while (j >= gap && nums[j - gap] > temp) {",
        "        nums[j] = nums[j - gap];",
        "        j -= gap;",
        "      }",
        "      nums[j] = temp;",
        "    }",
        "  }",
        "}",
      ],
      lineMap: LM_SHELL,
    },
    python: {
      code: [
        "def shell_sort(nums):",
        "    n = len(nums)",
        "    gap = n // 2",
        "    while gap > 0:",
        "        for i in range(gap, n):",
        "            temp = nums[i]",
        "            j = i",
        "            while j >= gap and nums[j - gap] > temp:",
        "                nums[j] = nums[j - gap]",
        "                j -= gap",
        "            nums[j] = temp",
        "        gap //= 2",
        "",
      ],
      lineMap: { init: 2, loop: 4, visit: 5, compare: 8, update: 9, done: 12 },
    },
  };
}

function langsQuick() {
  return {
    cpp: {
      code: [
        "void quickSort(vector<int>& nums, int lo, int hi) {",
        "  if (lo >= hi) return;",
        "  int pivot = nums[hi];",
        "  int i = lo;",
        "  for (int j = lo; j < hi; j++) {",
        "    if (nums[j] <= pivot) {",
        "      swap(nums[i], nums[j]);",
        "      i++;",
        "    }",
        "  }",
        "  swap(nums[i], nums[hi]);",
        "  quickSort(nums, lo, i - 1);",
        "  quickSort(nums, i + 1, hi);",
        "}",
      ],
      lineMap: LM_QUICK,
    },
    java: {
      code: [
        "void quickSort(int[] nums, int lo, int hi) {",
        "  if (lo >= hi) return;",
        "  int pivot = nums[hi];",
        "  int i = lo;",
        "  for (int j = lo; j < hi; j++) {",
        "    if (nums[j] <= pivot) {",
        "      swap(nums, i, j);",
        "      i++;",
        "    }",
        "  }",
        "  swap(nums, i, hi);",
        "  quickSort(nums, lo, i - 1);",
        "  quickSort(nums, i + 1, hi);",
        "}",
      ],
      lineMap: LM_QUICK,
    },
    javascript: {
      code: [
        "function quickSort(nums, lo = 0, hi = nums.length - 1) {",
        "  if (lo >= hi) return;",
        "  const pivot = nums[hi];",
        "  let i = lo;",
        "  for (let j = lo; j < hi; j++) {",
        "    if (nums[j] <= pivot) {",
        "      [nums[i], nums[j]] = [nums[j], nums[i]];",
        "      i++;",
        "    }",
        "  }",
        "  [nums[i], nums[hi]] = [nums[hi], nums[i]];",
        "  quickSort(nums, lo, i - 1);",
        "  quickSort(nums, i + 1, hi);",
        "}",
      ],
      lineMap: LM_QUICK,
    },
    python: {
      code: [
        "def quick_sort(nums, lo, hi):",
        "    if lo >= hi:",
        "        return",
        "    pivot = nums[hi]",
        "    i = lo",
        "    for j in range(lo, hi):",
        "        if nums[j] <= pivot:",
        "            nums[i], nums[j] = nums[j], nums[i]",
        "            i += 1",
        "    nums[i], nums[hi] = nums[hi], nums[i]",
        "    quick_sort(nums, lo, i - 1)",
        "    quick_sort(nums, i + 1, hi)",
        "",
      ],
      lineMap: { init: 2, loop: 6, visit: 7, compare: 8, update: 10, done: 13 },
    },
  };
}

function langsBucket() {
  return {
    cpp: {
      code: [
        "void bucketSort(vector<double>& nums) {",
        "  int n = nums.size();",
        "  vector<vector<double>> b(n);",
        "  for (double x : nums)",
        "    b[min(n - 1, (int)(x * n))].push_back(x);",
        "  int k = 0;",
        "  for (auto& bucket : b) {",
        "    sort(bucket.begin(), bucket.end());",
        "    for (double x : bucket) nums[k++] = x;",
        "  }",
        "}",
      ],
      lineMap: LM_BUCKET,
    },
    java: {
      code: [
        "void bucketSort(double[] nums) {",
        "  int n = nums.length;",
        "  ArrayList<Double>[] b = new ArrayList[n];",
        "  for (double x : nums)",
        "    b[Math.min(n - 1, (int)(x * n))].add(x);",
        "  int k = 0;",
        "  for (ArrayList<Double> bucket : b) {",
        "    Collections.sort(bucket);",
        "    for (double x : bucket) nums[k++] = x;",
        "  }",
        "}",
      ],
      lineMap: LM_BUCKET,
    },
    javascript: {
      code: [
        "function bucketSort(nums) {",
        "  const n = nums.length;",
        "  const buckets = Array.from({ length: n }, () => []);",
        "  for (const x of nums)",
        "    buckets[Math.min(n - 1, Math.floor(x * n))].push(x);",
        "  let k = 0;",
        "  for (const bucket of buckets) {",
        "    bucket.sort((a, b) => a - b);",
        "    for (const x of bucket) nums[k++] = x;",
        "  }",
        "}",
      ],
      lineMap: LM_BUCKET,
    },
    python: {
      code: [
        "def bucket_sort(nums):",
        "    n = len(nums)",
        "    buckets = [[] for _ in range(n)]",
        "    for x in nums:",
        "        bi = min(n - 1, int(x * n))",
        "        buckets[bi].append(x)",
        "    k = 0",
        "    for bucket in buckets:",
        "        bucket.sort()",
        "        for x in bucket:",
        "            nums[k] = x",
        "            k += 1",
        "",
      ],
      lineMap: { init: 2, loop: 5, compare: 6, update: 8, visit: 10, done: 13 },
    },
  };
}

function langsGap() {
  return {
    cpp: {
      code: [
        "int maximumGap(vector<int>& nums) {",
        "  sort(nums.begin(), nums.end());",
        "  int best = 0;",
        "  for (int i = 1; i < nums.size(); i++)",
        "    best = max(best, nums[i] - nums[i - 1]);",
        "  return best;",
        "}",
      ],
      lineMap: LM_GAP,
    },
    java: {
      code: [
        "int maximumGap(int[] nums) {",
        "  Arrays.sort(nums);",
        "  int best = 0;",
        "  for (int i = 1; i < nums.length; i++)",
        "    best = Math.max(best, nums[i] - nums[i - 1]);",
        "  return best;",
        "}",
      ],
      lineMap: LM_GAP,
    },
    javascript: {
      code: [
        "function maximumGap(nums) {",
        "  nums.sort((a, b) => a - b);",
        "  let best = 0;",
        "  for (let i = 1; i < nums.length; i++)",
        "    best = Math.max(best, nums[i] - nums[i - 1]);",
        "  return best;",
        "}",
      ],
      lineMap: LM_GAP,
    },
    python: {
      code: [
        "def maximum_gap(nums):",
        "    nums.sort()",
        "    best = 0",
        "    for i in range(1, len(nums)):",
        "        best = max(best, nums[i] - nums[i - 1])",
        "    return best",
        "",
      ],
      lineMap: { init: 2, loop: 4, compare: 5, update: 6, visit: 5, done: 7 },
    },
  };
}

export const SORTING_ORDER = [
  "bubble-sort",
  "selection-sort",
  "insertion-sort",
  "iterative-merge-sort",
  "counting-sort",
  "heap-sort",
  "radix-sort",
  "shell-sort",
  "sort-an-array",
  "bucket-sort",
  "maximum-gap",
];

export const SORTING_PROBLEMS = {
  "bubble-sort": {
    title: "Bubble Sort",
    difficulty: "Easy",
    category: "Sorting",
    timeComplexity: "O(n²)",
    spaceComplexity: "O(1)",
    visualizer: "sorting",
    description: "Sort by repeatedly swapping adjacent elements if they are out of order.",
    example: { input: "nums = [5,3,8,4,2]", output: "[2,3,4,5,8]", note: "Each pass pushes the largest unsorted value to its final slot." },
    defaultInput: { nums: [5, 3, 8, 4, 2] },
    inputFields: ["nums"],
    explanation: [
      { emoji: "💡", title: "Idea", body: "Like shaking heavier bubbles to the surface — compare neighbors and swap inversions." },
      { emoji: "⚡", title: "Complexity", body: "Two nested loops ⇒ Θ(n²) time and O(1) extra space." },
    ],
    languages: langsBubble(),
  },
  "selection-sort": {
    title: "Selection Sort",
    difficulty: "Easy",
    category: "Sorting",
    timeComplexity: "O(n²)",
    spaceComplexity: "O(1)",
    visualizer: "sorting",
    description: "Repeatedly select the smallest element from the unsorted suffix and swap it forward.",
    example: { input: "nums = [64,25,12,22,11]", output: "[11,12,22,25,64]", note: "Exactly n swaps in the classic variant." },
    defaultInput: { nums: [64, 25, 12, 22, 11] },
    inputFields: ["nums"],
    explanation: [
      { emoji: "💡", title: "Idea", body: "Grow a sorted prefix by dragging the next minimum into place." },
      { emoji: "⚡", title: "Complexity", body: "Always scans the remainder ⇒ Θ(n²) time, O(1) space." },
    ],
    languages: langsSelection(),
  },
  "insertion-sort": {
    title: "Insertion Sort",
    difficulty: "Easy",
    category: "Sorting",
    timeComplexity: "O(n²)",
    spaceComplexity: "O(1)",
    visualizer: "sorting",
    description: "Build a sorted prefix by inserting each next element like sorting playing cards.",
    example: { input: "nums = [5,2,4,6,1,3]", output: "[1,2,3,4,5,6]", note: "Great when data is nearly sorted." },
    defaultInput: { nums: [5, 2, 4, 6, 1, 3] },
    inputFields: ["nums"],
    explanation: [
      { emoji: "💡", title: "Idea", body: "Shift larger elements right until the key fits — stable and adaptive." },
      { emoji: "⚡", title: "Complexity", body: "Worst Θ(n²); best Θ(n) when already sorted." },
    ],
    languages: langsInsertion(),
  },
  "iterative-merge-sort": {
    title: "Merge Sort (Iterative)",
    difficulty: "Medium",
    category: "Sorting",
    timeComplexity: "O(n log n)",
    spaceComplexity: "O(n)",
    visualizer: "sorting",
    description: "Bottom-up merge sort: merge subarrays of doubling width until the whole array is sorted.",
    example: { input: "nums = [38,27,43,3,9,82,10]", output: "[3,9,10,27,38,43,82]", note: "Avoids recursion depth; needs scratch buffer for merges." },
    defaultInput: { nums: [38, 27, 43, 3, 9, 82, 10] },
    inputFields: ["nums"],
    explanation: [
      { emoji: "💡", title: "Idea", body: "Treat each element as sorted, merge pairs, then pairs of pairs." },
      { emoji: "⚡", title: "Complexity", body: "Θ(n log n) time with O(n) auxiliary space for merges." },
    ],
    languages: langsMerge(),
  },
  "counting-sort": {
    title: "Counting Sort",
    difficulty: "Easy",
    category: "Sorting",
    timeComplexity: "O(n + k)",
    spaceComplexity: "O(k)",
    visualizer: "sorting",
    description: "Count frequencies of keys in a bounded integer range, prefix-sum for ranks, scatter back stably.",
    example: { input: "nums = [4,2,2,8,3,3,1]", output: "[1,2,2,3,3,4,8]", note: "k = max − min + 1 must be manageable." },
    defaultInput: { nums: [4, 2, 2, 8, 3, 3, 1] },
    inputFields: ["nums"],
    explanation: [
      { emoji: "💡", title: "Idea", body: "Histogram keys, turn counts into ending indices, write right-to-left for stability." },
      { emoji: "⚡", title: "Complexity", body: "Linear in n and range width k — not comparison-based." },
    ],
    languages: langsCounting(),
  },
  "heap-sort": {
    title: "Heap Sort",
    difficulty: "Medium",
    category: "Sorting",
    timeComplexity: "O(n log n)",
    spaceComplexity: "O(1)",
    visualizer: "sorting",
    description: "Turn the array into a max-heap, then repeatedly swap the root with the heap tail and sift down.",
    example: { input: "nums = [12,11,13,5,6,7]", output: "[5,6,7,11,12,13]", note: "In-place but not stable." },
    defaultInput: { nums: [12, 11, 13, 5, 6, 7] },
    inputFields: ["nums"],
    explanation: [
      { emoji: "💡", title: "Idea", body: "Heap guarantees the largest unsorted element is always at index 0." },
      { emoji: "⚡", title: "Complexity", body: "Build heap O(n); each extract + sift is O(log n)." },
    ],
    languages: langsHeap(),
  },
  "radix-sort": {
    title: "Radix Sort",
    difficulty: "Medium",
    category: "Sorting",
    timeComplexity: "O(d · (n + k))",
    spaceComplexity: "O(n + k)",
    visualizer: "sorting",
    description: "Least-significant-digit radix sort with base-10 buckets (demo assumes non-negative integers).",
    example: { input: "nums = [170,45,75,90,802,24,2,66]", output: "[2,24,45,66,75,90,170,802]", note: "Stable digit passes preserve earlier ties." },
    defaultInput: { nums: [170, 45, 75, 90, 802, 24, 2, 66] },
    inputFields: ["nums"],
    explanation: [
      { emoji: "💡", title: "Idea", body: "Sort by each decimal position from ones upward using a stable subroutine." },
      { emoji: "⚡", title: "Complexity", body: "d digits, base k buckets ⇒ O(d · (n + k))." },
    ],
    languages: langsRadix(),
  },
  "shell-sort": {
    title: "Shell Sort",
    difficulty: "Medium",
    category: "Sorting",
    timeComplexity: "O(n^1.3)",
    spaceComplexity: "O(1)",
    visualizer: "sorting",
    description: "Insertion sort with a gap sequence (visual uses diminishing gaps n/2… — Knuth variant in steps).",
    example: { input: "nums = [12,34,54,2,3]", output: "[2,3,12,34,54]", note: "Much faster than plain insertion on random data." },
    defaultInput: { nums: [12, 34, 54, 2, 3] },
    inputFields: ["nums"],
    explanation: [
      { emoji: "💡", title: "Idea", body: "Early passes swap far-apart elements so later insertion passes do less work." },
      { emoji: "⚡", title: "Complexity", body: "Depends on gap sequence; simplified analysis often cites roughly O(n^{3/2})." },
    ],
    languages: langsShell(),
  },
  "sort-an-array": {
    title: "Quicksort",
    difficulty: "Medium",
    category: "Sorting",
    timeComplexity: "O(n log n)",
    spaceComplexity: "O(log n)",
    visualizer: "sorting",
    description: "Partition around a pivot (Lomuto variant with pivot at end of range), recurse on both sides.",
    example: { input: "nums = [5,2,3,1]", output: "[1,2,3,5]", note: "Random pivots help avoid worst-case Θ(n²) on sorted input." },
    defaultInput: { nums: [5, 2, 3, 1] },
    inputFields: ["nums"],
    explanation: [
      { emoji: "💡", title: "Idea", body: "Place pivot so everything left is ≤ pivot and everything right is ≥ pivot." },
      { emoji: "⚡", title: "Complexity", body: "Average O(n log n) time, O(log n) stack space for recursion." },
    ],
    languages: langsQuick(),
  },
  "bucket-sort": {
    title: "Bucket Sort",
    difficulty: "Medium",
    category: "Sorting",
    timeComplexity: "O(n)",
    spaceComplexity: "O(n)",
    visualizer: "sorting",
    description: "Distribute values in [0,1) across n buckets, sort each bucket, then concatenate.",
    example: { input: "nums = [0.42,0.32,0.23,0.52,0.25]", output: "sorted ascending", note: "Assumes uniform-ish distribution for linear behavior." },
    defaultInput: { nums: [0.42, 0.32, 0.23, 0.52, 0.25] },
    inputFields: ["nums"],
    explanation: [
      { emoji: "💡", title: "Idea", body: "Bucket boundaries spread values so each bucket stays tiny." },
      { emoji: "⚡", title: "Complexity", body: "Linear expected time when buckets stay balanced." },
    ],
    languages: langsBucket(),
  },
  "maximum-gap": {
    title: "Maximum Gap",
    difficulty: "Hard",
    category: "Sorting",
    timeComplexity: "O(n)",
    spaceComplexity: "O(n)",
    visualizer: "sorting",
    description: "Maximum difference between successive elements in sorted order (visual sorts then scans gaps).",
    example: { input: "nums = [3,6,9,1]", output: "3", note: "Linear-time pigeonhole solution exists off-screen." },
    defaultInput: { nums: [3, 6, 9, 1] },
    inputFields: ["nums"],
    explanation: [
      { emoji: "💡", title: "Idea", body: "Sorted adjacency gaps encode spacing — scan for the largest jump." },
      { emoji: "⚡", title: "Complexity", body: "Interview optimal uses buckets; this demo uses sort + scan for clarity." },
    ],
    languages: langsGap(),
  },
};

export const SORTING_PROB_LIST = SORTING_ORDER.map((id) => {
  const p = SORTING_PROBLEMS[id];
  return {
    id,
    title: p.title,
    difficulty: p.difficulty,
    category: p.category,
    desc: p.description,
    tags: ["sorting", "arrays"],
  };
});
